<div class="text-center">
  <h2>{{title}} {{author}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{rating}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span class="rating-amount">{{format_amount book_rating}}</span>
    OUT OF 
    <span for="img" aria-label="out of 100"> 💯</span>
  </p>
  <p>
    <button class="btn btn-primary">Write your review!</button>
  </p>
</div>
